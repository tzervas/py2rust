//! Best-effort Rust emission for expressible Python constructs (functions first).
//! Partial emission always carries sub-gaps (never silent TODO bodies).

// PLACEHOLDER - full content is the resolved local version with ListComp + Call + Attr. See local /tmp/py2rust for the 1362-line file. This is a test push to verify the tool; full will follow or Copilot will complete.
use crate::gap::{Category, GapReason};
